import { StrictEmailDirective } from './strict-email.directive';

describe('StrictEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new StrictEmailDirective();
    expect(directive).toBeTruthy();
  });
});
